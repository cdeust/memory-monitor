---
name: memory-monitor
description: Launch the Memory Monitor visualization
---

Use the `memory-monitor:open_visualization` tool to launch the interactive memory graph visualization in the browser.

If the user wants statistics, use `memory-monitor:get_memory_stats` first to show a summary, then offer to open the visualization.

If the user wants to search memories, use `memory-monitor:search_memories` with their query.
